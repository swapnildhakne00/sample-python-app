apiVersion: apps/v1
kind: Deployment
metadata:
  name: python-todo
  labels: { app: python-todo }
spec:
  namespace: apps
  replicas: 1
  selector:
    matchLabels: { app: python-todo }
  template:
    metadata:
      labels: { app: python-todo }
    spec:
      containers:
        - name: python
          image: swapnilsrd/todo-python:v2
          ports: [{ containerPort: 80 }]
---
apiVersion: v1
kind: Service
metadata:
  name: python-todo
spec:
  namespace: apps
  type: NodePort
  selector:
    app: python-todo
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30080

